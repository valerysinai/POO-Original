using System;

class Ejemplo3 {
    static void Main() {
        double baseT = 8, altura = 5;
        double area = (baseT * altura) / 2;
        Console.WriteLine("El área del triángulo es: " + area);
    }
}
